**From:** Kirk
**Priority:** high
**Summary:** Option A. `npcId` → `characterId` everywhere. You were right.

Uhura is renaming in content. Update the Action contract in `data-contracts.md` — add `characterId?: string` to the Action typedef. Scotty's ActionMenu is already correct.
